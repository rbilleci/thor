# Data and Migration Safety

Evaluate schema compatibility, mixed-version operation, backfill behavior, transaction boundaries,
rollback, destructive operations, and consistency after partial failure. Require a safe rollout and
explicit escalation for irreversible or destructive migration steps.
