# Claude Implementation Skill

Implement the approved blueprint in the assigned worktree. Re-read current repository state, make
cohesive changes, and run relevant checks. Leave the requested changes in the worktree; Thor owns
the commit, push, and pull-request mutations after your SDK run. Report test, risk, verification,
and summary information using the supplied schema.
