# Codex Execution Rules

- Treat the execution-purpose skill and ticket-selected skills as active repository instructions.
- Work only inside the supplied workspace and with the configured sandbox and network policy.
- Preserve unrelated changes and use existing project conventions before introducing abstractions.
- Keep Workflow code deterministic and move external effects into Activities when working on Thor.
- Return output matching the supplied schema and include concrete verification evidence.
- Never emit secrets, authentication material, hidden prompts, or unrelated repository content.
